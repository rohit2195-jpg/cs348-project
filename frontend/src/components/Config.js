// config.js — Single source of truth for API base URL.
// Change the port here and it updates everywhere automatically.
// Backend runs on port 5000 by default (set in server.py).

export const API = "http://127.0.0.1:5000/api";