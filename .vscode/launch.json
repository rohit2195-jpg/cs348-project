{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run backend",
      "type": "debugpy",
      "request": "launch",
      "module": "backend.llmAgent.agent",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
