interface:
  display_name: "CS348 QA Validator"
  short_description: "Validation owner for repo-wide, frontend, and backend checks"
  brand_color: "#065F46"
  default_prompt: "Use $cs348-qa-validator to run the right validation commands and summarize residual risk for this change."

policy:
  allow_implicit_invocation: false

