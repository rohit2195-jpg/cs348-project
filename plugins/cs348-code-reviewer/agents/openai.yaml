interface:
  display_name: "CS348 Code Reviewer"
  short_description: "Review diffs for regressions, risk, and missing tests"
  brand_color: "#991B1B"
  default_prompt: "Use $cs348-code-reviewer to review this change for bugs, regressions, validation issues, and missing tests."

policy:
  allow_implicit_invocation: false

